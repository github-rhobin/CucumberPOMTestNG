package enums;

public enum ConfigProp {
	
	// config.properties keys
	RUN_MODE,
	BROWSER_HEADLESS,
	BROWSER_HEIGHT,
	BROWSER_WIDTH,
	RETRY_FAILED_TEST,
	PAGE_LOAD_TIMEOUT_TIMER,
	EXPLICIT_WAIT_TIMER,
}
